{
  "name": "proyecto-finca-hm",
  "version": "1.0.0",
  "description": "Root package for Finca HM project",
  "scripts": {
    "start": "docker-compose up -d && npx concurrently \"cd backend && npm run start:dev\" \"cd frontend && npm run dev\""
  }
}
